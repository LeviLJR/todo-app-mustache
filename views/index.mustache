<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3"
      crossorigin="anonymous"
    />
    <title>TODO APP</title>
  </head>
  <body>
    <div class="container">
      <h1 class="mt-4">TODO APP</h1>
      <h2 class="mt-4">Tarefas</h2>
      <ul class="list-group">
        {{#tasks}}
        <li
          class="list-group-item d-flex justify-content-between align-items-center"
        >
          <span>
            {{ name }}
            <span class="{{ priorityClass }} ms-2">{{ priority }}</span>
          </span>

          <span>
            <a href="/?tid={{ id }}" class="btn btn-primary btn-sm">edit</a>
            <a href="/tarefas/del/{{ id }}" class="btn btn-danger btn-sm"
              >done</a
            >
          </span>
        </li>
        {{/tasks}}
      </ul>
      <a class="mt-4 btn btn-primary" href="/sorted">Ver lista ordenada</a>
      <a class="mt-4 btn btn-primary" href="/count">Quantidade de tarefas</a>

      <h2 class="mt-4">Nova tarefa</h2>
      <form action="/tarefas" method="POST" class="mb-4">
        {{#task.id}}
        <input type="hidden" name="id" value="{{.}}" />
        {{/task.id}}
        <div class="mb-3">
          <label for="nome" class="form-label">Nome:</label>
          <input
            type="text"
            id="nome"
            name="nome"
            class="form-control"
            value="{{ task.name }}"
            required
          />
        </div>
        <div class="mb-3">
          <label for="prioridade" class="form-label">Prioridade:</label>
          <select
            id="prioridade"
            name="prioridade"
            class="form-select"
            value="task.priority"
            required
          >
            <option disabled selected>Selecione</option>
            <option class="text-sucess" name="Baixa">Baixa</option>
            <option class=".text-warning" name="Média">Média</option>
            <option class=".text-danger" name="Alta">Alta</option>
          </select>
        </div>
        <input type="submit" value="Salvar" class="btn btn-primary" />
      </form>
      <p>{{ erro }}</p>
    </div>

    <!-- Bootstrap Bundle with Popper -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
